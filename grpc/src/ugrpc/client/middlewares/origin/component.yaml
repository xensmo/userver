type: object
description: gRPC client origin middleware component
additionalProperties: false
properties:
    user-agent:
        type: string
        description: The name of the current deploy unit that will appear in `x-origin`
            metadata
        defaultDescription: Do not send `x-origin`
